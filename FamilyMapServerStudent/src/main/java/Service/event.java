package Service;
import model.Event;
public class event {
    Event[] events() {
        // returns all events associated with user
        Event[] userEvents = null;
        return userEvents;
    }
}
