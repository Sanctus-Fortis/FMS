package Service;
import model.Event;

/***
 * Service for event retrieval. Returns an array of events.
 */
public class event {
    /***
     * Places all events associated with a user into an array and return it as an Event array.
     * @return An array of event objects.
     */
    Event[] events() {
        // returns all events associated with user
        Event[] userEvents = null;
        return userEvents;
    }
}
