package Service;

/***
 * Service for loading user history from the DB
 */
public class load {
    /***
     * Loads user history from the DB and populates application
     */
    void loadContent() {
        // clear database
        // loads user, person and event data from request into DB
    }
}
