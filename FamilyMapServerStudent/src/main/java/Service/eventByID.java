package Service;
import model.Event;

/***
 * Service for retrieval of a single event by ID, returns an event.
 */
public class eventByID {
    /***
     * Finds a single event by its ID and returns it.
     * @return an Event object
     */
    Event userEvent() {
        // find a single event by its ID and return it
        Event returnEvent = null;
        return returnEvent;
    }
}
