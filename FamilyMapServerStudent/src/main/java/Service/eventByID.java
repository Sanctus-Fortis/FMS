package Service;
import model.Event;
public class eventByID {
    Event userEvent() {
        // find a single event by its ID and return it
        Event returnEvent = null;
        return returnEvent;
    }
}
